[build]
  command = "npm install && npm run build"
  publish = "dist"

# Ensure SPA routes (e.g. /admin-login) work on refresh.
# This is also covered by public/_redirects, but keeping it here is harmless.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
